<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for network access in release mode (injected automatically in debug) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Voice-note recording in the comments compose bar (audio_waveforms). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Biometric sign-in gate (local_auth). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Avatar picking (image_picker) and task-submission files (file_picker)
         intentionally declare NO permissions: image_picker rides the system
         photo picker (Android 13+) / ACTION_GET_CONTENT and camera-capture
         intents, and file_picker rides the Storage Access Framework — none
         of which need READ_MEDIA_* or CAMERA. Declaring CAMERA here would
         *impose* a runtime grant requirement on the capture intent. -->
    <!-- FCM / system notifications (Android 13+). Unlike the picker/camera
         entries above, this one *does* need a runtime grant — request it via
         FcmService.requestPermission() at a product-chosen moment, not at
         cold start (especially important on iOS, where the prompt is once-ever). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="Skiflux"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Default small icon for FCM notification trays. Without this,
             Android renders a solid grey square. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- App Links / Deep Linking -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="app.skiflux.com" />
                <data android:scheme="https" android:host="skiflux.com" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: resolve browsers for external checkout/mission/help
             links, and mail apps for the support composer. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>


