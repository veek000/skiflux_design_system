# Backend Integration Points

> Auto-generated from `// TODO(backend, ...)` comments in the codebase.
> Do not edit this file directly — update the in-code tags and
> regenerate using: `grep -rn "TODO(backend" lib/`
>
> Generated: 2026-07-28
> Total tags: 32 (27 blocking, 5 minor)
>
> Down from 43 (38 blocking, 5 minor). Eleven blocking tags resolved since the
> last generation: the native social `id_token` flow landed, so the
> `auth_chrome.dart` tag is gone — replaced by a new *minor* one in
> `social_auth.dart` for the Apple Service ID that Android's web fallback needs.
> The home feed, tasks catalog, wallet ledger + processor reference, FCM device
> registration, video feed card thumbnail, edit-profile load and avatar upload,
> and My Profile's real identity + auth gate all now read real data. One minor
> tag resolved (`auth_flow.dart`, the signed-in address for the biometric
> frame). Two grep hits (`auth_endpoints.dart:4`, `social_auth.dart:61`) are
> prose referring to these tags rather than tags themselves, and are excluded
> from the count as before.

## Blocking

### Home

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/home/data/comments_store.dart` | 149 | replace seeded demo comments with real per-video comments fetched from backend, and persist sent comments/voicenotes server-side | `List<{author: SkifluxCommentAuthor, body: SkifluxCommentBody, authorName: String, handle: String, message: String?, audioPath: String?, timeLabel: String}>` |
| `lib/features/home/full_screen_player_screen.dart` | 45 | replace local placeholder asset paths with real CDN/backend video thumbnail URLs | `String (network URL)` |

### Leaderboard

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/leaderboard/data/leaderboard_store.dart` | 64 | replace static leaderboard rankings with live weekly ranking data fetched from backend | `List<{rank: int, name: String, username: String, initials: String, xp: int}> plus currentUserRank: int and betterThanPercent: int` |

### Notifications

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/notifications/data/notifications_store.dart` | 39 | replace static _seed() notifications list with real per-user notification feed fetched from backend | `List<{title: String, body: String, icon: String, time: DateTime, action: String?, unread: bool}>` |

### Playlists

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/playlists/data/playlists_store.dart` | 47 | replace local coverAsset file path with real CDN/backend playlist cover image URL | `String (network URL)` |
| `lib/features/playlists/data/playlists_store.dart` | 69 | replace static kCoinPacks + kCoinRateNaira with backend-driven pricing and available coin pack offerings | `List<{coins: int, priceNaira: int, badge: CoinPackBadge, savePercent: int?}> plus rateNairaPerCoin: int` |
| `lib/features/playlists/data/playlists_store.dart` | 128 | replace in-memory SkillCoin wallet (balance, playlists, episode lock states) with real backend | `{skillCoins: int, playlists: List<{id: String, title: String, creatorName: String, creatorUsername: String, episodes: List<{id: String, number: int, title: String, duration: String, coinCost: int, state: PlaylistEpisodeState}>, description: String, viewsLabel: String, coverUrl: String}>}` |
| `lib/features/playlists/playlist_episode_row.dart` | 163 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String (network URL)` |

### Profile

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/profile/badges_screen.dart` | 22 | replace static badge list with real per-user badge progress fetched from backend | `List<{assetUrl: String, caption: String, earned: bool}>` |
| `lib/features/profile/data/skill_world_store.dart` | 105 | persist the selected SkillWorld on the authenticated user profile instead of session-only state | `{world: String} on GET/PATCH profile` |
| `lib/features/profile/downloads_screen.dart` | 27 | replace session-local downloads list with real offline download storage and download state from backend | `List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, fileSizeMb: int, qualityLabel: String}>` |
| `lib/features/profile/library_episode_row.dart` | 110 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String (network URL)` |
| `lib/features/profile/liked_videos_screen.dart` | 25 | replace session-local liked list with real per-user liked videos fetched from backend | `List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, isNew: bool, postedToday: bool, watchProgress: double}>` |
| `lib/features/profile/liked_videos_screen.dart` | 218 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String (network URL)` |
| `lib/features/profile/my_profile_screen.dart` | 566 | replace local placeholder asset with real CDN/backend video thumbnail URL | `String (network URL)` |
| `lib/features/profile/profile_screen.dart` | 33 | replace hardcoded creator profile identity (name, handle, avatar initials) with real creator data from backend | `{name: String, handle: String, initials: String, subscribed: bool, notifyPreference: NotifyPreference}` |
| `lib/features/profile/profile_screen.dart` | 427 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String (network URL)` |
| `lib/features/profile/public_user_profile_screen.dart` | 81 | replace PublicUserProfile.demo() and hardcoded defaults with real user profile data fetched from backend | `{name: String, username: String, initials: String?, league: String, xp: int, leaderboardRank: int, tasksDone: int, email: String, skills: List<String>, badges: List<{label: String, assetUrl: String}>, completedTasks: List<{kind: String, title: String, meta: String, actionLabel: String, score: int?, band: String?, bandDetail: String?, passed: bool?}>}` |
| `lib/features/profile/saved_videos_screen.dart` | 25 | replace session-local saved list with real per-user saved/bookmarked videos from backend | `List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, isNew: bool, postedToday: bool, watchProgress: double}>` |
| `lib/features/profile/watch_history_screen.dart` | 50 | replace session-local watch history with real per-user watch history fetched from backend | `List<{episode: {epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String}, today: bool, progress: double}>` |

### Search

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/search/data/search_index.dart` | 124 | replace local demo search dataset with backend search API | `search(query: String) → {episodes: List<{epNumber: int, title: String, duration: String, views: String, creator: String}>, creators: List<{name: String, username: String, subscribers: String}>, users: List<{name: String, username: String, subscribers: String}>, playlists: List<{title: String, creator: String, episodeCount: int, duration: String}>}` |

### Settings

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/settings/data/payment_store.dart` | 8 | replace with tokenized cards from the payment processor (never store PANs) | `{cards: List<{brand: String, last4: String, expiry: String}>}` |
| `lib/features/settings/data/settings_store.dart` | 10 | persist and sync these preferences with the backend user-settings record | `{notifications: Map<String,bool>, biometricLogin: bool, twoFactorAuth: bool, autoPlayNext: bool, downloadQuality: String, downloadOnWifiOnly: bool, appLanguage: String, saveWatchHistory: bool, personalisedRecommendations: bool}` |

### Streaks

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/streaks/data/streaks_store.dart` | 137 | replace static streak stats and history with real per-user streak data fetched from backend | `{streak: int, bestStreak: int, xpEarned: int, milestone: int, milestoneXp: int, history: List<{start: DateTime, days: List<{label: String, state: StreakDayState, number: int?}>}>}` |

### Subscriptions

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/subscriptions/data/subscriptions_store.dart` | 172 | replace static seeded creators and episodes with real per-user subscription data from backend | `{creators: List<{name: String, username: String, initials: String, notificationMode: CreatorNotificationMode, hasUnseen: bool}>, episodes: List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, isNew: bool, postedToday: bool, watchProgress: double}>}` |
| `lib/features/subscriptions/subscription_widgets.dart` | 263 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String (network URL)` |

### Wallet

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/wallet/transaction_details_screen.dart` | 76 | open a dispute for this entry | `POST /wallet/transactions/{reference}/report → {caseId}` |

## Minor

### Auth

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/auth/data/social_auth.dart` | 182 | supply an Apple Service ID + redirect URI so Sign in with Apple can run on Android via WebAuthenticationOptions | Service ID (e.g. `com.skiflux.web`) and the `https` return URL registered with Apple |
| `lib/features/auth/screens/biometric_screen.dart` | 171 | draw the account's avatar from the cached profile | `{avatar} on GET /api/v1/me` |

### Profile

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/profile/my_profile_screen.dart` | 453 | persist watch-history removals once the rail reads a real watch-history feed instead of the subscriptions feed | `DELETE /watch-history/{episodeId}` |

### Search

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/search/search_result_widgets.dart` | 60 | replace brand-colored placeholder thumbnails with real episode/playlist cover artwork URLs from backend | `String (network URL)` |

### Shared

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/shared/sheets/share_sheet.dart` | 26 | replace static demo share targets with real OS share sheet backed by actual deep links and content URLs from backend | `TBD, no current placeholder structure to infer from` |
