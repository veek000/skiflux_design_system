# Backend Integration Points

> Auto-generated from `// TODO(backend, ...)` comments in the codebase.
> Do not edit this file directly — update the in-code tags and
> regenerate using: `grep -rn "TODO(backend" lib/`
>
> Generated: 2026-07-22
> Total tags: 29 (27 blocking, 2 minor)

## Blocking

### Notifications

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/notifications/data/notifications_store.dart` | 40 | replace static _seed() notifications list with real per-user notification feed fetched from backend | `List<{title: String, body: String, icon: String, time: DateTime, action: String?, unread: bool}>` |

### Leaderboard

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/leaderboard/data/leaderboard_store.dart` | 64 | replace static leaderboard rankings with live weekly ranking data fetched from backend | `List<{rank: int, name: String, username: String, initials: String, xp: int}>` plus `currentUserRank: int` and `betterThanPercent: int` |

### Streaks

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/streaks/data/streaks_store.dart` | 125 | replace static streak stats and history with real per-user streak data fetched from backend | `{streak: int, bestStreak: int, xpEarned: int, milestone: int, milestoneXp: int, history: List<{start: DateTime, days: List<{label: String, state: StreakDayState, number: int?}>}>}` |

### Playlists

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/playlists/data/playlists_store.dart` | 47 | replace local coverAsset file path with real CDN/backend playlist cover image URL | `String` (network URL) |
| `lib/features/playlists/data/playlists_store.dart` | 70 | replace static kCoinPacks + kCoinRateNaira with backend-driven pricing and available coin pack offerings | `List<{coins: int, priceNaira: int, badge: CoinPackBadge, savePercent: int?}>` plus `rateNairaPerCoin: int` |
| `lib/features/playlists/data/playlists_store.dart` | 132 | replace in-memory SkillCoin wallet (balance, playlists, episode lock states) with real backend | `{skillCoins: int, playlists: List<{id: String, title: String, creatorName: String, creatorUsername: String, episodes: List<{id: String, number: int, title: String, duration: String, coinCost: int, state: PlaylistEpisodeState}>, description: String, viewsLabel: String, coverUrl: String}>}` |
| `lib/features/playlists/playlist_episode_row.dart` | 165 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String` (network URL) |

### Search

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/search/data/search_index.dart` | 124 | replace local demo search dataset with backend search API | `search(query: String) → {episodes: List<{epNumber: int, title: String, duration: String, views: String, creator: String}>, creators: List<{name: String, username: String, subscribers: String}>, users: List<{name: String, username: String, subscribers: String}>, playlists: List<{title: String, creator: String, episodeCount: int, duration: String}>}` |

### Subscriptions

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/subscriptions/data/subscriptions_store.dart` | 174 | replace static seeded creators and episodes with real per-user subscription data from backend | `{creators: List<{name: String, username: String, initials: String, notificationMode: CreatorNotificationMode, hasUnseen: bool}>, episodes: List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, isNew: bool, postedToday: bool, watchProgress: double}>}` |
| `lib/features/subscriptions/subscription_widgets.dart` | 271 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String` (network URL) |

### Tasks

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/tasks/data/tasks_store.dart` | 196 | replace static seeded learning tasks and missions with real per-user task catalog from backend, including submission persistence and quiz answer recording | `{learning: List<{id: String, title: String, description: String, episodeLabel: String, episodeTitle: String, episodeSubtitle: String, status: LearningTaskStatus, kind: LearningTaskKind, coins: int, xp: int, feedback: String?, briefIntro: String?, briefBullets: List<String>, quiz: QuizData?, quizAnswers: List<int?>?, quizCorrect: int?}>, missions: List<{id: String, title: String, description: String, coins: int, actionLabel: String, iconKey: String, completed: bool}>}` |

### Home / Comments

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/home/data/comments_store.dart` | 151 | replace seeded demo comments with real per-video comments fetched from backend, and persist sent comments/voicenotes server-side | `List<{author: SkifluxCommentAuthor, body: SkifluxCommentBody, authorName: String, handle: String, message: String?, audioPath: String?, timeLabel: String}>` |

### Wallet

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/wallet/data/wallet_store.dart` | 124 | replace static seeded transaction ledger and bank account with real backend wallet history and withdrawal destination | `{transactions: List<{title: String, subtitle: String, delta: int, type: CoinTxnType, kind: CoinTxnKind}>, defaultBank: {bankName: String, accountNumber: String, holderName: String}?}` |

### Profile

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/profile/my_profile_screen.dart` | 27 | replace hardcoded user identity (name, handle, initials, XP, world, rank) with real authenticated user profile data from backend | `{name: String, handle: String, initials: String, xp: int, world: String, leaderboardRank: int}` |
| `lib/features/profile/my_profile_screen.dart` | 69 | gate entire My Profile tab behind real authentication — currently no auth layer exists; user identity is hardcoded demo data | TBD, no current placeholder structure to infer from |
| `lib/features/profile/my_profile_screen.dart` | 400 | replace local placeholder asset with real CDN/backend video thumbnail URL | `String` (network URL) |
| `lib/features/profile/public_user_profile_screen.dart` | 91 | replace PublicUserProfile.demo() and hardcoded defaults with real user profile data fetched from backend | `{name: String, username: String, initials: String?, league: String, xp: int, leaderboardRank: int, tasksDone: int, email: String, skills: List<String>, badges: List<{label: String, assetUrl: String}>, completedTasks: List<{kind: String, title: String, meta: String, actionLabel: String, score: int?, band: String?, bandDetail: String?, passed: bool?}>}` |
| `lib/features/profile/profile_screen.dart` | 33 | replace hardcoded creator profile identity (name, handle, avatar initials) with real creator data from backend | `{name: String, handle: String, initials: String, subscribed: bool, notifyPreference: NotifyPreference}` |
| `lib/features/profile/profile_screen.dart` | 433 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String` (network URL) |
| `lib/features/profile/badges_screen.dart` | 22 | replace static badge list with real per-user badge progress fetched from backend | `List<{assetUrl: String, caption: String, earned: bool}>` |
| `lib/features/profile/downloads_screen.dart` | 27 | replace session-local downloads list with real offline download storage and download state from backend | `List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, fileSizeMb: int, qualityLabel: String}>` |
| `lib/features/profile/liked_videos_screen.dart` | 25 | replace session-local liked list with real per-user liked videos fetched from backend | `List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, isNew: bool, postedToday: bool, watchProgress: double}>` |
| `lib/features/profile/liked_videos_screen.dart` | 221 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String` (network URL) |
| `lib/features/profile/saved_videos_screen.dart` | 26 | replace session-local saved list with real per-user saved/bookmarked videos from backend | `List<{epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String, isNew: bool, postedToday: bool, watchProgress: double}>` |
| `lib/features/profile/watch_history_screen.dart` | 52 | replace session-local watch history with real per-user watch history fetched from backend | `List<{episode: {epNumber: int, title: String, creatorUsername: String, duration: String, views: String, postedAgo: String}, today: bool, progress: double}>` |

### Shared

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/shared/widgets/video_feed_card.dart` | 42 | replace local placeholder asset paths with real CDN/backend video thumbnail URLs | `String` (network URL) |
| `lib/features/profile/library_episode_row.dart` | 110 | replace local placeholder asset with real CDN/backend episode thumbnail URL | `String` (network URL) |

## Minor

### Search

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/features/search/search_result_widgets.dart` | 60 | replace brand-colored placeholder thumbnails with real episode/playlist cover artwork URLs from backend | `String` (network URL) |

### Shared

| File | Line | Description | Expected Shape |
|------|------|-------------|-----------------|
| `lib/shared/sheets/share_sheet.dart` | 26 | replace static demo share targets with real OS share sheet backed by actual deep links and content URLs from backend | TBD, no current placeholder structure to infer from |
